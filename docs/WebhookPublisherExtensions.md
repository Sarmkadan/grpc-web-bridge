# WebhookPublisherExtensions

Provides extension methods for managing and interacting with webhook subscriptions and event publishing in the `grpc-web-bridge` project. This static class facilitates subscribing to events with filtering, publishing events asynchronously, retrieving subscription statistics, and querying existing subscriptions.

## API

### `SubscribeWithFilter`
